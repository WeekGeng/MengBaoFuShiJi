<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.administrator.mengbaofushiji.view.TalkDetailActivity">
    <include layout="@layout/toolbar"></include>
    <ListView
        android:layout_width="match_parent"
        android:layout_below="@+id/toolbar"
        android:layout_height="match_parent"></ListView>
    <LinearLayout
        android:layout_width="match_parent"
        android:background="#fff"
        android:layout_alignParentBottom="true"
        android:id="@+id/share_huifu_input_liner"
        android:layout_height="wrap_content">
        <EditText
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:textColorHint="@color/transparent_gray"
            android:hint="说点什么吧....."
            android:padding="6dp"
            android:textCursorDrawable="@null"
            android:focusable="true"
            android:layout_gravity="center"
            android:textColor="@color/gray"
            android:layout_margin="5dp"
            android:id="@+id/share_dialog"
            android:background="@drawable/cicle_back_talk_detail_send"
            />
        <ImageView
            android:id="@+id/send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/send"
            android:textColor="#fff"
            android:layout_marginLeft="5dp"
            android:layout_gravity="center"
            android:padding="10dp"
            android:background="@drawable/cicle_back_talk_detail_send"
            android:layout_marginRight="5dp"
            />
    </LinearLayout>
</RelativeLayout>
