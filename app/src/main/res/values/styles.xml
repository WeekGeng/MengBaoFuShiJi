<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">#4876FF</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">#3A5FCD</item>
        <!-- 窗口的背景颜色 -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- SearchView -->
        <item name="searchViewStyle">@style/MySearchViewStyle</item>
    </style>
    <!--<style name="Theme.Smartkids" parent="AppTheme.Base">-->
    <!--</style>-->
    <!--<style name="AppTheme" parent="@style/AppBaseTheme"></style>-->
    <style name="AppTheme" parent="AppTheme.Base"></style>
    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@color/theme_color</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/theme_color</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">@color/theme_color</item>
        <item name="colorAccent">@color/color_red</item>
        <!--Window color-->
        <item name="android:windowBackground">@android:color/white</item>
        <!--Indicator-->
        <!-- edittext-->
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/theme_color</item>
        <item name="colorControlHighlight">@color/theme_color</item>
    </style>
    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">

    </style>
    <style name="AppTheme.Search" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:actionModeBackground">@color/theme_color</item>
        <item name="actionModeBackground">@color/theme_color</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/theme_color</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">@color/theme_color</item>
        <item name="colorAccent">@color/color_red</item>
        <!--Window color-->
        <item name="android:windowBackground">@android:color/white</item>
        <!--Indicator-->
        <!-- edittext-->
        <item name="colorControlNormal">@color/theme_color</item>
        <item name="colorControlActivated">@color/color_white</item>
        <item name="colorControlHighlight">@color/color_white</item>
    </style>
</resources>